1. Install dependencies 
2. Make sure you have a json server named as db.json in your root, for more info:https://github.com/typicode/json-server#getting-started
3. Run server script
4. Run dev script
